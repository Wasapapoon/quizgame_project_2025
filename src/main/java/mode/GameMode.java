package mode;

/**
 * The GameMode enum defines the available difficulty levels and play styles
 * within the application. It is used to configure puzzle sets, time limits,
 * and UI layouts throughout the game session.
 */
public enum GameMode {
	EASY, MEDIUM, HARD, BATTLE
}
